<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodportioningapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalculateProportion"
            android:parentActivityName = ".MainActivity"/>
        <activity android:name=".RecipeBook"
            android:parentActivityName = ".MainActivity"/>
        <activity android:name=".AddRecipe"
            android:parentActivityName = ".MainActivity"/>
        <activity android:name=".AddIngredient"
            android:parentActivityName = ".AddRecipe"/>
        <activity android:name=".AddRecipeSecond"
            android:parentActivityName = ".MainActivity"/>
        <activity android:name=".AddIngredientSecond"
            android:parentActivityName = ".AddRecipeSecond"/>
    </application>

</manifest>